name: Qualité du quiz

on:
  push:
    branches: [main, 'refactor/**']
  pull_request:
    branches: [main]

jobs:
  quality:
    name: Validation contenu et build
    runs-on: ubuntu-latest

    steps:
      - name: Récupérer le dépôt
        uses: actions/checkout@v4

      - name: Installer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Installer les dépendances
        run: npm ci

      - name: Calibrer les fréquences d'apparition
        run: PRINT_APPEARANCE_JSON=1 APPEARANCE_SAMPLES=50000 npm run sim:build-appearance

      - name: Valider le contenu
        run: npm run content:validate

      - name: Valider la rareté hybride
        run: npm run rarity:validate

      - name: Vérifier les types
        run: npm run typecheck

      - name: Construire l'application
        run: npm run build
